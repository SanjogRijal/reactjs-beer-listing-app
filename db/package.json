{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for Beers",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch beerdb.json --port 3004",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
